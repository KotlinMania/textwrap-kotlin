# Port targets — `textwrap-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `textwrap` that codex-rs uses, and therefore the minimum subset that `textwrap-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-152] (rank `266`, tier `direct`, status `To Do`)

**Cargo package name**: `textwrap`
**Rust module name (in `use` statements)**: `textwrap`

## Summary

- **3** distinct symbols imported by codex-rs
- **4** total `use` statements
- **4** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `textwrap::Options`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/bottom_pane/textarea.rs:33` (`codex-tui`) — `use textwrap::Options`
- `tui/src/wrapping.rs:33` (`codex-tui`) — `use textwrap::Options`

### `textwrap::WordSplitter`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/exec_cell/render.rs:28` (`codex-tui`) — `use textwrap::WordSplitter`

### `textwrap::wrap`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-tui`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `tui/src/bottom_pane/app_link_view.rs:19` (`codex-tui`) — `use textwrap::wrap`
